import styles from '../../styles/about/analoguePhotography.module.scss';

export const AnaloguePhotography = () => {

    return (
        <section className={styles.analoguePhotography}>
            <div className={styles.backgroundImage} />
        </section>
    )
}